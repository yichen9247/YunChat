package com.android.yunchat.enmu

enum class FileTypeEnum { IMAGE, VIDEO, AVATAR, FILE }
enum class LoginModeEnum { LOGIN, REGISTER }
enum class StorageTypeEnum { FILE, IMAGE, VIDEO, AVATAR }
enum class InputDialogType { SEARCH_GROUP, EDIT_NICK, EDIT_PASSWORD }